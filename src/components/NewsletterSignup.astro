<div>
  <div class="text-center">
    <h2 class="text-2xl sm:text-3xl font-bold tracking-tight bg-gradient-to-r from-primary-600 to-secondary-600 bg-clip-text text-transparent">Stay Updated</h2>
    <p class="mt-2 sm:mt-4 text-base sm:text-lg text-gray-600">
      Get notified about new opportunities directly in your inbox.
    </p>
  </div>
  <form class="mt-6 sm:mt-8 max-w-md mx-auto">
    <div class="flex flex-col sm:flex-row gap-4">
      <label for="email-address" class="sr-only">Email address</label>
      <input
        id="email-address"
        name="email"
        type="email"
        autocomplete="email"
        required
        class="min-w-0 flex-auto rounded-md border-0 px-3.5 py-2 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-primary-600 text-sm sm:text-base sm:leading-6"
        placeholder="Enter your email"
      />
      <button
        type="submit"
        class="flex-none rounded-md bg-gradient-to-br from-primary-600 to-primary-700 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:from-primary-700 hover:to-primary-800 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600 transition-all duration-300"
      >
        Subscribe
      </button>
    </div>
  </form>
</div>